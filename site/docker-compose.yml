services:
  quiz-json-builder:
    build: .
    container_name: quiz-json-builder
    ports:
      - "8080:80"
    environment:
      - REPO_URL=https://github.com/Francois389/Quiz
    restart: unless-stopped
